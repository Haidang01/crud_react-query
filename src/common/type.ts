export type Product = {
    id: number
    name: string
    quantity: number
    image: string
    description: string
    price: number
}

export type User = {
    id: number
    username: string
    email: string
    password: string
}
